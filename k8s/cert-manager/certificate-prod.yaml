apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: masterops-public-cert
  namespace: ingress-system
spec:
  secretName: masterops-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${MASTEROPS_HOST}